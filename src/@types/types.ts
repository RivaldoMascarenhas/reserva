export type FieldType = {
  nameReservation?: string;
  Guests?: number;
  data?: Date;
  time?: Date;
};
export interface valuesProps {
  company: string;
  name: string;
  email: string;
  password: string;
  confirmPassword: string;
  agree: boolean;
}
