export type FieldType = {
  nameReservation?: string;
  equipment?: string;
  data?: Date;
  time?: Date;
  description?: string;
};
export interface valuesProps {
  company: string;
  name: string;
  email: string;
  password: string;
  confirmPassword: string;
  agree: boolean;
}
